# 复制为 .env 或在系统环境变量中配置（不要提交 .env 到 GitHub）
DASHSCOPE_API_KEY=sk-e8b260dd87fc4aaa8937a7cda19ea56f

# 可选：模型名，默认 qwen-plus；省额度可改为 qwen-turbo
# DASHSCOPE_MODEL=qwen-turbo

# 设为 1 时不调用百炼，只做 CSV 合并 + 规则分类
# SKIP_LLM=1
