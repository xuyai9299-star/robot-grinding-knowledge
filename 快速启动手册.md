# 🚀 机器人柔性磨削知识库 · 快速启动手册

> 最后更新：2025年6月 · 项目路径：`d:\robot-grinding-knowledge`

---

## 一、启动方式

### 🖱️ 方式 A：双击 bat 文件（最简单）

直接双击项目根目录的 **`run_demo.bat`**，服务器自动启动，浏览器自动打开 `http://localhost:8765`。

### ⌨️ 方式 B：命令行（3 步）

**第 1 步：安装依赖（仅首次）**

```powershell
cd d:\robot-grinding-knowledge
pip install -r requirements.txt
```

**第 2 步：配置 AI Key（可选但推荐）**

```powershell
py -3 setup_api.py
# 粘贴百炼 API Key（sk-xxxxxxxx）→ 回车 → 完成
```

> 不配也能用，AI 搜索和智能筛选降级为规则模式。
> 获取 Key：https://help.aliyun.com/zh/model-studio/first-api-call-to-qwen

**第 3 步：启动服务器**

```powershell
py -3 pipeline_server.py
```

看到 `Running on http://0.0.0.0:8765` 后 → 浏览器打开 **http://localhost:8765**

| 页面 | 地址 |
|------|------|
| 🏠 知识库主站 | http://localhost:8765 |
| 🎓 答辩Dashboard | http://localhost:8765/dashboard/ |

---

### ⚠️ 为什么不能双击 HTML 打开？

所有页面需要从**服务器**访问（`http://localhost:8765/xxx.html`），不能直接双击文件打开（`file:///D:/.../xxx.html`）。

原因：页面内部用 `fetch()` 加载 JSON 数据（论文索引、分类统计等），浏览器安全策略禁止 `file://` 协议发起网络请求。通过服务器访问即可正常运行。

---

## 二、全部页面地址一览

所有页面通过主站导航栏进入，也可直接访问：

| 页面 | 地址 |
|------|------|
| 🏠 **知识库主站** | http://localhost:8765 |
| 🎓 答辩Dashboard | http://localhost:8765/dashboard/ |
| 🧠 D3 径向树 | http://localhost:8765/d3_mindmap.html |
| 📊 vis-network 层级导图 | http://localhost:8765/mindmaps.html |
| 🎯 ECharts 径向树 | http://localhost:8765/radial.html |
| 🌌 3D 知识星系 | http://localhost:8765/galaxy.html |
| 📚 会议与期刊 | http://localhost:8765/conferences/ |
| 📈 全屏时间轴 | http://localhost:8765/timeline.html |
| 🔍 高级检索 | http://localhost:8765/search.html |
| 📊 检索实验数据 | http://localhost:8765/experiment.html |
| 🎓 答辩流程图 | http://localhost:8765/defense_flowchart.html |

> 子页面导航已统一：思维导图 4 页互相跳转（D3/层级/ECharts/星系），其余仅保留「← 返回主页」。

---

## 三、知识库更新（一键自动）

### 方式 A：网页按钮（推荐）

1. 打开 http://localhost:8765
2. 滚动到底部「🔄 知识库自动更新」
3. 点击「🚀 开始自动更新」
4. 实时终端显示 5 步进度：爬取 → LLM筛选 → 去重合并 → 自动分类 → 重建数据
5. 完成后显示新增论文列表和被拒绝的不相关论文

### 方式 B：命令行

```powershell
# 增量入库（Zotero CSV → 千问摘要 → 分类 → 前端数据）
py -3 ingest.py

# 仅重建前端数据（不爬取新论文）
py -3 ingest.py --rebuild-only

# 强制全部重新分类
py -3 ingest.py --reclassify-all
```

---

## 四、AI 搜索说明

主站 Hero 区的搜索栏是 **AI 驱动的智能搜索**：

```
用户提问 → 混合检索（五维分类加成+关键词匹配）→ Top-3 论文 → 千问生成回答
```

- 回答中标注 `[#ID]` 引用，点击可跳转到论文详情
- 搜索策略基于检索实验优化：分类命中 +0.8，摘要命中 +0.3，Recall 提升 29%
- 未配 API Key 时降级为关键词检索 + 论文列表展示
- 也可通过导航栏「🔍 高级检索」进入三栏式精确检索

---

## 五、项目核心文件

| 文件/目录 | 说明 |
|-----------|------|
| `papers_enriched_updated.json` | 主数据库（1247+ 篇论文 + 五维标签 + 中文摘要） |
| `papers_index.json` | 前端论文索引（详情/外链） |
| `mindmaps_five.json` | 五维思维导图数据 |
| `mindmap_stats.json` | 分类统计（工艺树/应用/材料） |
| `conferences/conferences.json` | 期刊/会议数据库 |
| `conferences/timeline.json` | 年份趋势数据 |
| `web/` | 前端静态文件（10 个 HTML + JS/CSS/JSON） |
| `chroma_db/` | ChromaDB 向量数据库（1024 维 embedding） |
| `.env` | API Key 配置（由 setup_api.py 生成） |

### Python 脚本

| 脚本 | 作用 |
|------|------|
| `pipeline_server.py` | Flask 服务器（AI搜索 + 知识库更新 + 静态文件） |
| `setup_api.py` | 一键配置百炼 API Key |
| `ingest.py` | 增量入库入口（CSV → LLM → 分类 → 前端数据） |
| `scripts/crawl_papers.py` | OpenAlex API 爬取（20 组关键词） |
| `scripts/taxonomy.py` | 五维分类引擎（中英双语关键词匹配） |
| `scripts/build_outputs.py` | 前端数据文件生成（8 个 JSON/HTML/MM） |
| `scripts/build_vector_db.py` | ChromaDB 向量库构建（text-embedding-v3） |
| `scripts/experiment_retrieval.py` | 检索策略对比实验（3 策略 × 20 题） |
| `scripts/pipeline_runner.py` | 全自动更新管线引擎（SSE 进度推送） |
| `scripts/bailian_client.py` | 百炼千问调用封装（摘要/关键词生成） |

---

## 六、常见问题

**Q: 双击 HTML 文件打开是白屏？**
A: 正常现象。页面需要从服务器加载 JSON 数据，浏览器禁止 `file://` 协议发起 fetch 请求。请通过 `http://localhost:8765` 访问。

**Q: 最简单的启动方式？**
A: 双击 `run_demo.bat`，服务器自动启动 + 浏览器自动打开。

**Q: 网页打开后 AI 搜索没反应？**
A: 确认是用 `pipeline_server.py` 启动的（端口 8765）。如果用 `http.server`（端口 8080）启动，AI 搜索和知识库更新不可用。

**Q: AI 搜索返回「无法连接后端」？**
A: 检查终端窗口是否还在运行。端口被占用时：`netstat -ano | findstr 8765` 找到进程后终止。

**Q: 页面还是旧的/没有进度条/没有粒子背景？**
A: 按 `Ctrl+Shift+R` 强制刷新（跳过浏览器缓存），或打开无痕窗口。

**Q: 知识库更新时 LLM 显示「规则兜底」？**
A: 运行 `py -3 setup_api.py` 配置百炼 Key，或检查 `.env` 文件是否存在。
