# 开发者素材需求清单

> 发送给正在构建向量库的同学
> 用于：PPT 素材 + 实验报告 + 答辩展示
> 优先级：🔴 必须 > 🟡 重要 > 🟢 加分

---

## 你现在在做什么

你正在构建向量库（`build_vector_db.py`，百炼 text-embedding-v3 → ChromaDB）。

我需要你把**整个过程的关键节点**记录下来，用于 PPT 和实验报告。

---

## 🔴 必须提供（缺了 PPT 没法做）

### 1. 向量库构建过程的截图/录屏

**要求**：
- 录屏软件：Windows 自带 Win+G（Xbox Game Bar），或 OBS
- 截图工具：Win+Shift+S
- **必须截/录的内容**：

| 节点 | 内容 | 格式 |
|------|------|------|
| 运行开始 | 终端显示 "VECTOR DATABASE BUILDER" + 1247 papers loaded | 截图 |
| 进度条 | 跑了一半时（50%左右）的进度条 `[####------] 50%` | 截图 |
| 构建完成 | 终端显示 "BUILD COMPLETE" + Papers embedded: 1247 | 截图 |
| 测试查询 | 终端显示的 sample query 结果（返回了哪3篇论文） | 截图 |
| chroma_db 文件夹 | 资源管理器窗口，显示 chroma_db/ 目录和文件大小 | 截图 |

> 如果能**全程录屏**（3-5分钟），就不需要截图。录屏文件发给我。

### 2. 向量库构建的关键数据

请把这些数字填好发给我：

```
向量库构建报告
─────────────────
论文总数：       ______ 篇
有效论文(有文本)： ______ 篇
Embedding 模型：  百炼 text-embedding-v3
向量维度：       1024
单次 API 耗时：   ______ 秒/篇
总耗时：          ______ 分钟
失败/超时：       ______ 篇
数据库大小：      ______ MB
数据库路径：      chroma_db/
```

### 3. 检索测试：用 5 个问题测试向量库

在 ChromaDB 里跑这 5 个查询，把 **Top-3 结果**截屏或复制给我：

| # | 查询问题 | 你要给我的 |
|---|---------|-----------|
| 1 | "钛合金叶片磨削表面粗糙度控制" | Top-3 论文标题 + 相似度分数 + 截图 |
| 2 | "砂带磨损状态监测方法" | 同上 |
| 3 | "机器人磨削力控制策略" | 同上 |
| 4 | "高温合金 Inconel 718 磨削参数" | 同上 |
| 5 | "磨削残余应力预测模型" | 同上 |

**怎么跑**：

```python
import chromadb
client = chromadb.PersistentClient(path="chroma_db")
collection = client.get_collection("robot_grinding_papers")

queries = [
    "钛合金叶片磨削表面粗糙度控制",
    "砂带磨损状态监测方法",
    "机器人磨削力控制策略",
    "高温合金 Inconel 718 磨削参数",
    "磨削残余应力预测模型",
]

for q in queries:
    r = collection.query(query_texts=[q], n_results=3)
    print(f"\n查询: {q}")
    for i, (doc_id, dist, meta) in enumerate(zip(r["ids"][0], r["distances"][0], r["metadatas"][0])):
        print(f"  #{i+1}: [{dist:.3f}] {meta.get('title','?')[:60]}")
```

把终端输出截图发给我。

---

## 🟡 重要提供（实验报告核心数据）

### 4. Chunk 策略对比实验

向量库建好之后，做三组实验，每组用同样的 20 个问题测试：

| 实验组 | Chunk 方式 | 怎么跑 |
|--------|-----------|--------|
| A：整篇 | title+abstract+keywords 拼接为一个向量 | 默认向量库（已建好） |
| B：分字段 | abstract 单独向量化 + keywords 单独向量化 | 新建 collection，分两个字段分别 embed |
| C：层级索引 | 先按 L1-L3 分类过滤，再在子集内检索 | 不改向量库，检索时加分类过滤 |

**需要的数据**（每组一个表格）：

```
实验组 A：整篇 Chunk
─────────────────
Recall@5：  ______%
Recall@10： ______%
MRR：       ______
平均耗时：   ______秒/查询
```

三组对比表：

| 策略 | Recall@5 | Recall@10 | MRR | 耗时 |
|------|----------|-----------|-----|------|
| A: 整篇 | ___% | ___% | ___ | ___s |
| B: 分字段 | ___% | ___% | ___ | ___s |
| C: 层级索引 | ___% | ___% | ___ | ___s |

> 实验代码参考 `D:\robot-grinding-knowledge\scripts\experiment_retrieval.py`，把 BM25 换成 ChromaDB 查询即可。

### 5. 按问题类型的细分数据

把 20 个问题按类型分组，给出每组的三策略对比：

| 问题类型 | A-整篇 | B-分字段 | C-层级 | 最优 |
|---------|--------|---------|--------|------|
| 工艺查询 | ___% | ___% | ___% | ___ |
| 材料对比 | ___% | ___% | ___% | ___ |
| 趋势分析 | ___% | ___% | ___% | ___ |
| 定义解释 | ___% | ___% | ___% | ___ |

---

## 🟢 加分提供（答辩锦上添花）

### 6. RAG 对话 Demo 录屏

如果你已经把检索+生成管线跑通了：

- 录一段 **30 秒**的屏幕录像：用户在聊天框输入问题 → 系统返回带引用的回答
- 问 3 个问题展示不同类型（工艺查询 + 材料对比 + 趋势分析）

### 7. 架构图

如果你能做一张系统架构图（用 draw.io / PPT / Figma 都行）：

```
用户提问 → Embedding → ChromaDB检索 → 结构化重排序 → Prompt拼接 → 百炼生成 → 回答
```

---

## 📦 最终交付物清单

| # | 文件 | 格式 | 优先级 |
|---|------|------|--------|
| 1 | 构建过程截图（≥5张）或录屏（3-5分钟） | PNG / MP4 | 🔴 |
| 2 | 构建关键数据（论文数/耗时/大小） | 文字 | 🔴 |
| 3 | 5个测试查询的 Top-3 结果截图 | PNG | 🔴 |
| 4 | 三组 Chunk 对比实验数据表 | Excel/文字 | 🟡 |
| 5 | 按问题类型细分数据表 | Excel/文字 | 🟡 |
| 6 | RAG 对话 Demo 录屏（如有） | MP4 | 🟢 |
| 7 | 系统架构图（如有） | PNG | 🟢 |

---

## ⏰ 建议顺序

```
今天：     跑 build_vector_db.py + 截屏/录屏 + 跑5个测试查询
明天上午： 做三组 Chunk 对比实验 + 整理数据表
明天下午： 把所有素材打包发给我
```

**所有截图请截完整窗口（包括终端标题栏），不要只截文字部分。**
**录屏请横屏 1920×1080，确保文字清晰可读。**

有问题随时问我。
